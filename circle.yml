dependencies:
  override:
    - curl https://sh.rustup.rs -sSf | sh
    - rustup update stable
    - cargo install rustfmt
test:
  override:
    - cargo clean
    - cargo fmt -- --write-mode=diff
    - cargo build --verbose
