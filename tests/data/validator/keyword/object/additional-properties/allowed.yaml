schema:
  properties:
    - name:
        type: string
  additionalProperties: true
tests:
  - valid: true
    description: Must be valid if there're no additional properties
    data:
      name: Foo
  - valid: true
    description: Must be valid if there're additional properties
    data:
      name: Foo
      age: 10
