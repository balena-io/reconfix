schema:
  version: 1
  type: integer
  multipleOf: 5
tests:
  - valid: true
    description: Must be valid if the remainder equals to zero
    data: 10
  - valid: false
    description: Must be invalid if the remainder doesn't equal to zero
    data: 11
